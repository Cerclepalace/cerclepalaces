# apps/admin

Back-office plateforme — shops, clients, coursiers, commandes, livraisons, produits, conformité, paiements, commissions, QR codes, zones, incidents, statistiques, paramètres.

> Emplacement réservé — à construire sur le socle `packages/domain` et `packages/db`.
