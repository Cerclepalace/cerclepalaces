# apps/client

Interface client — accueil, géolocalisation, recherche de shops, page shop, catalogue, fiche produit, panier, adresse, paiement, suivi temps réel, historique, compte, notifications, support.

> Emplacement réservé — à construire sur le socle `packages/domain` et `packages/db`.
