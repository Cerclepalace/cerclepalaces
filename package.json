{
  "name": "cbd-marketplace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Plateforme de livraison pour CBD shops physiques — marketplace, acquisition par QR, réseau de coursiers, back-office.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
